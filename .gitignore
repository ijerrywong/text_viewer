# macOS
.DS_Store
._*

# 编辑器 / 工具运行时状态
.vmark/
.idea/
.vscode/
# Claude Code 的本地会话检查点
.claude/
*.swp
# WorkBuddy 的会话记忆：内容是 AGENTS.md 与 docs/ 的复述，且会各自漂移
# （例如 MEMORY.md 仍写着「mp-html 打底」，而 verify-notes.md §11 早已决策自研）。
# 权威口径一律以 AGENTS.md + docs/ 为准，不把陈旧副本入库。
.workbuddy/

# Node
node_modules/
npm-debug.log*
yarn-error.log*

# 微信小程序
# 构建产物：由「工具 → 构建 npm」生成，不入库
miniprogram/miniprogram_npm/
# 私有配置：含本机路径与个人设置，微信官方建议不提交
project.private.config.json

# 测试语料中的大文件与真实 AI 输出样本
# （HTML 类语料需自行收集，体积大且可能含个人内容）
tests/fixtures/*.large.*
tests/fixtures/private/

# 构建/验证产物
dist/
*.log
