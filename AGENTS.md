# AGENTS.md — 微信「纯文本阅读器」项目上下文与硬约束

> 本文件是给 **WorkBuddy（腾讯 AI 桌面工作台）/ CodeBuddy** 以及任何接手本项目的 AI 编码代理阅读的项目宪法。
> 任何实现行为都必须服从本文件的约束；当实现与本文件冲突时，以本文件为准，并在提交信息里说明理由。

---

## 1. 产品一句话

一个 **纯本地、零后端** 的微信小程序：在微信里打开、查看、浏览 **HTML / Markdown / TXT / 其他纯文本**（JSON、CSV、日志、源码等）。

> 关键澄清：这里的 HTML **不是网页**，而是 AI 生成的**自包含长文档**（书籍、文章、PDF/PPT 转写）：没有可执行 JS、没有后端接口、CSS 内联或 `<style>` 内嵌、图片多为 base64 内嵌或相对路径已丢失。
>
> ⚠️ 但要修正一个常见的错误假设：AI 生成的 HTML **并不总是"CSS 内联"**。相当高比例引用 `cdn.tailwindcss.com` 并只写类名——这类文档在小程序里会样式全丢。处理方案见 §3.1。

### 1.1 三个入口，优先级不同

| 入口 | 定位 | 说明 |
|---|---|---|
| `wx.chooseMessageFile` | **保底入口，一定可用** | 从当前会话选文件。必须由用户点击触发，无法自动调起，不能访问收藏或手机文件系统 |
| **剪贴板粘贴** | **AI 时代最高频场景，不可省略** | 大量用户不是在传文件，而是从 Claude/ChatGPT 复制一大段 Markdown，手机上无处渲染。零文件、零权限、零等待，应放首屏显眼位置。**必须由用户明确点击「粘贴」才读取**——iOS 会弹"读取了你的剪贴板"系统提示，静默读取是体验灾难。粘贴内容默认不落盘，另给显式「保存到最近」 |
| 聊天素材打开（scene 1173） | **体验上限，但需验证** | 用户在聊天里直接点文件→用本小程序打开。见 §2.7，**不得作为主流程依赖** |

> 用户的自然直觉是"在聊天里点开文件→找用小程序打开"。若 scene 1173 验证不通过（或未上线前），这条路走不通，用户必须反过来"先进小程序→再选聊天文件"。这个认知落差是最大流失点，需用首次引导图示 + 首屏大按钮 + 引导添加到「我的小程序」来对冲。

## 2. 核心约束（不可违反）

### 2.1 零后端 / 本地优先
- **禁止**引入任何自建服务端、云函数、数据库、对象存储。
- 所有文件读取、解析、渲染、持久化都在小程序端完成。
- 理由：AI 文档可能敏感（隐私）、离线可用、零服务器成本、规避 ICP/内容审核合规负担。

### 2.2 只读查看器，不修改原文件
- 对用户文件**只读**。任何"编辑/另存/导出"都不在 MVP 范围内（可作远期规划，但不得默认实现）。

### 2.3 平台硬限制（实现时必须遵守）
| 项 | 限制 | 说明 |
|---|---|---|
| 主包体积 | ≤ 2MB | 主包只放入口 + TXT 渲染 + 核心检测 |
| 总包体积 | ≤ 20MB（含分包） | 渲染库/GBK 码表/高亮器进分包 |
| **`setData` 单次** | **≤ 1MB，实践控制在 64KB** | **渲染大文档最直接的一堵墙。** 逻辑层→Native→视图层三跳，代价极高。必须分块下发，用路径写法局部更新（`'blocks[3].nodes'`），绝不整份 setData |
| key-value 存储 | 约 10MB 总 / 单 key 1MB | 只存元数据、阅读进度、设置，**不存正文** |
| 用户文件目录 | `wx.env.USER_DATA_PATH` | 用 `FileSystemManager` 落盘，配额平台相关（约 200MB，需真机验证） |
| **Worker 代码包** | **≤ 2MB** | 码表不打包进 Worker，由主线程 `postMessage` 传入 |
| **Worker 可用 API** | **无 `wx.getFileSystemManager()`** | 见 2.6，决定整条数据流走向 |
| `wx.chooseMessageFile` | 从聊天选文件 | 主要文件入口（保底入口，一定可用） |
| 聊天素材打开（scene 1173） | 需 `app.json` 声明 `supportedMaterials` | 体验最优入口，**需真机 + 提审验证**，见 2.7 |
| `rich-text` | HTML 子集，无 script/iframe | 渲染层基础之一 |
| `web-view` | **个人主体不可用** | 微信不向个人主体开放业务域名，无申诉通道。**永远不要生成 web-view 相关代码** |

### 2.4 安全（最高优先级）
- HTML 必须**消毒**：剥离 `script`、`iframe`、`object`、`embed`、`form`、`base`、`meta[refresh]`、事件属性（on*）、`javascript:`/`vbscript:`/`data:text/html` 链接。
- 外链**禁止直接跳转**：点击外链 → 弹窗确认 → 复制到剪贴板（`wx.setClipboardData`）。小程序本就无法跳转外部浏览器，复制是唯一解。
- **不加载外部 CSS / JS / 字体**：域名白名单 + 不执行脚本双重阻断，本就不可能加载。
- **网络图片：默认关闭，用户可在设置中开启。**
  > 修正说明：`<image>` 组件的 `src` **不受合法域名白名单限制**（只有 `wx.request` / `downloadFile` 受限），网络图片技术上可正常显示。禁止的真正理由是**隐私**——加载外链图会向第三方暴露用户 IP 与阅读行为。因此做成默认关闭的开关 + 明确的隐私说明，而不是硬禁止：Markdown 引用图床极常见，一刀切会大面积丢图。
- 文件名必须清洗：防路径穿越（`../`、绝对路径、`:` `*` `?` 等非法字符）。磁盘上**一律以 `stableId` 命名**，原始文件名只存元数据。
- **防解析炸弹**：节点数上限 50,000、嵌套深度上限 100、解析超时 10s，超限截断并提示。

### 2.5 编码（中国大陆文本的关键风险）
- 必须支持：UTF-8（含 BOM）、**GBK/GB2312/GB18030**、Big5、UTF-16 LE/BE。
- 识别顺序：**BOM → HTML `<meta charset>` → UTF-8 合法性状态机 → GB18030/Big5 字节启发式打分 → 默认 UTF-8**。
- ⚠️ **小程序不提供 `TextDecoder` 全局对象**（不是"支持不稳定"，是根本没有）。不要写特性检测分支，直接内置 GB18030 码表自实现解码。
  - GB18030 双字节区约 23,940 码位，紧凑 `Uint16Array` + Base64 内嵌约 45KB；四字节区用算法映射，零额外体积。Big5 约 30KB。全部进按需分包。
- 解码后必须：**剥离 BOM**（且在 Markdown 解析之前，否则首行标题失效）、**归一换行符为 `\n`**（含老 Mac 单 `\r`）、清洗行首行尾零宽字符。
- **必须**提供手动编码切换入口（识别误判时的兜底）。自动识别不可能 100% 准，一键切换比把识别率从 95% 优化到 97% 更有价值。

### 2.6 性能
- 大文件（≥10MB）禁止一次性 `readFile` 全部内容进内存再全量渲染。
- 用 `readFile({position, length})` 分块读 + 虚拟滚动 + 渐进渲染。
- **解析放 Worker，但注意数据流方向**：
  > `wx.getFileSystemManager()` 在 Worker 内**不可用**。正确流程是 **主线程读文件并解码 → 把字符串 `postMessage` 给 Worker → Worker 纯计算解析 → 分批回传 IR**（每批约 50 个 Block，避免单次 postMessage 过大）。解析层代码必须写成**不依赖任何 wx API 的纯函数**，这样既能在 Worker 跑，也能在 Node 里直接跑测试语料——后者比在小程序里调试快一个数量级。
- **虚拟滚动的高度补偿（最容易做砸的一点）**：未渲染块用预估高度占位，渲染后回填真实高度。若修改的是**已渲染区域上方**的块高度，会改变上方总高度导致页面猛跳，**必须同步反向调整 `scrollTop` 做补偿**。
- **阅读进度存「块索引 + 块内比例」，绝不存 `scrollTop` 像素值**。字号、行距、主题任一变化，像素位置即失效；虚拟滚动下高度还会动态修正。
- 单文件超大（如 >50MB）降级为"纯文本分页阅读"。

### 2.7 待验证的关键前提（不得凭经验断言）
以下事项**必须联网核对官方文档 + 真机验证**后才能写进实现，结论记入 `docs/verify-notes.md`：

1. **聊天素材打开（scene 1173）**——已确认能力存在：`app.json` 声明 `supportedMaterials`（`materialType` 用 MIME 类型、`name` 必须含 `${nickname}`、`desc` ≤22 字、`path` 跳转页），启动参数经 `forwardMaterials` 拿到 `{type, name, path, size}`，提审时会审核声明合规性，**上线后入口才出现**。
   仍待验证：**是否支持 `text/markdown`**（官方列举的文档类型含 `.txt` `.html` `.csv` `.pdf` 等，`.md` 未明确）、**个人主体是否有限制**、开发版/体验版能否验证。
   > 这是产品体验上限所在，但**不得作为主流程依赖**：M1 先用 `chooseMessageFile` 打通闭环，此入口验证不通也不影响可用性。
2. `chooseMessageFile` 的 `count` 上限、单文件大小上限、`tempFilePath` 生命周期。
3. `USER_DATA_PATH` 实际配额（约 200MB 需复核）。
4. `mp-html` 对 `<style>` 选择器的实际支持程度（决定自研 CSS 层的工作量，见 ADR-3）。

## 3. 技术选型（默认，如需更换须写 ADR）
- 原生小程序（WXML/WXSS/JS），**不**用 Taro/uni-app —— 最大兼容 + WorkBuddy/CodeBuddy 小程序 IDE 原生支持。
- 渲染：HTML 用 `mp-html` 打底 + **自研预处理层**（见 §3.1）；Markdown 用 `mp-html` 的 markdown 插件或 `towxml`（Phase 2 用样例集实测定案，见 ADR-3）。
- GB18030/Big5 解码：精简码表自实现（**不要指望 `TextDecoder`**），进按需分包。
- 代码高亮：`highlight.js` 按语言裁剪（Top 20 语言约 80KB，全量 900KB 不可接受），进分包。

### 3.1 HTML 渲染：mp-html 打底 + 自研预处理层
**不要全自研 tokenizer，也不要指望 mp-html 独自搞定。** `mp-html` 内置了简易 CSS 解析器（标签/class/id 选择器），已覆盖基础场景；但 AI 生成的文档有几类它处理不了，需要在**送入 mp-html 之前**做一层预处理：

| 预处理项 | 为什么必须做 |
|---|---|
| **Tailwind 类名展开** | ⚠️ **AI 生成 HTML 的最高频失败模式**。大量文档用 `<script src="cdn.tailwindcss.com">` + 类名，小程序里脚本不执行、域名不通，**样式全部归零，精美文档塌成一堆左对齐裸文本**。需内置类名→CSS 声明的**规则生成器**（正则解析 `p-4`/`bg-slate-50`/`grid-cols-3` 等模式，而非穷举查表），体积可控在 20KB 内，仅在检测到 Tailwind 时异步加载。**优先级高于代码高亮。** |
| **CSS 变量求值** | `:root{--primary:#4f46e5}` + `var(--primary)` 是 AI 输出的高频写法，不求值则配色全丢。单层收集 + 自顶向下继承即可覆盖绝大多数情况 |
| **`::before`/`::after` 合成** | AI 常用伪元素做图标、引号、序号装饰。inline style 无法表达伪元素，但可把 `content` 合成为真实子节点插入首/尾。成本低、视觉收益大 |
| **视口钳制** | AI 生成的 HTML 几乎必然含 `max-width:1200px` 之类桌面容器假设，手机上横向溢出。注入优先级最低的兜底：`* { max-width:100%; box-sizing:border-box }`，并钳制根容器固定宽度。**`px` 不做换算**——小程序里 px 就是逻辑像素，语义与浏览器一致 |
| **`position:fixed`/`sticky` 降级** | 降为 `static` 回到文档流。固定侧边栏在手机上只会遮挡正文；对目录类内容，回流反而更合理 |

**先实测再动手**：Phase 3 开始前，用真实 AI 文档样例集实测 `mp-html` 的 `<style>` 支持程度，把结论写进 `docs/verify-notes.md`，据此决定自研层的边界。**不要凭想象决定全自研或全依赖。**

### 3.2 脚本剥离后必须给出可见降级，不能留空白
| 识别到 | 降级为 |
|---|---|
| Chart.js / ECharts / D3 | 占位卡片 + 保留邻近说明文本 |
| Mermaid | 等宽代码块 + 复制按钮 |
| reveal.js / impress.js | **每个 `<section>` 渲染成一张竖排卡片，幻灯片流式化**（PPT 类 HTML 的最佳产品化处理） |
| 数学公式 | 等宽块 + 复制按钮。KaTeX 依赖 DOM 与字体测量，小程序内不可行；诚实降级优于渲染出错位公式 |
| 外链 `<link rel=stylesheet>` | 顶部提示"部分样式无法加载" |

> 总原则：**降级必须可见**。用户看到"这里有张图表但显示不了"，远好于看到一片空白而怀疑文件坏了。

## 3.5 给 AI 编码代理的禁止清单（每次会话开始前重申）
AI 写小程序最大的失败模式是**写出 Web 代码**。以下每条写了必定运行失败：

| ❌ 禁止 | ✅ 替代 |
|---|---|
| `document` / `window` / 任何 DOM API | 无 DOM。解析成数据结构 + `wx:for` 渲染 |
| `innerHTML` / `DOMParser` / `querySelector` | 自研 tokenizer 或用 mp-html |
| `<div>` `<span>` `<p>` `<img>` `<a>` | `<view>` `<text>` `<image>` + 自定义点击 |
| `localStorage` / `sessionStorage` | `wx.setStorageSync` / `wx.getStorageSync` |
| `fetch` / `XMLHttpRequest` / `axios` | `wx.request`（本项目基本用不到） |
| Node 的 `fs` / `path` / `Buffer` / `crypto` | `wx.getFileSystemManager()`；hash 自实现 |
| `TextDecoder` / `TextEncoder` | 不存在，编码转换必须自研 |
| React / Vue / JSX 语法 | 原生 `wx:if` / `wx:for` / `Component()` |
| `web-view` / `<iframe>` | 个人主体不可用，永不生成 |
| `eval` / `new Function` | 运行时不允许 |
| 一次 `setData` 大对象 | 分块下发，单次 < 64KB |
| 深度递归自定义组件 | **节点树扁平化为线性列表**（带 parent 索引），超 ~15 层递归会性能崩溃 |
| 组件内用类选择器影响子组件 | 小程序有样式隔离，动态样式走 inline `style` |

**npm 依赖的特殊流程**：小程序用 npm 包不能直接 import，必须 `npm install` → 微信开发者工具「工具 → 构建 npm」→ 生成 `miniprogram_npm/` 后方可 require。**每次新增依赖都要重新构建**，这是最容易遗漏的一步，模块找不到时优先查这里。

## 4. 目录结构（目标形态）
```
text-viewer/
├── AGENTS.md
├── project.config.json
├── miniprogram/
│   ├── app.js / app.json / app.wxss
│   ├── pages/ (index 入口 / reader 阅读器 / settings 设置)
│   ├── components/ (virtual-list / code-block / toc / image-lazy ...)
│   ├── core/
│   │   ├── intake/      # 文件接入（chooseMessageFile / 启动场景 / 分享）
│   │   ├── detect/      # 类型 + 编码识别
│   │   ├── parse/       # txt / md / html → 统一文档模型(IR)
│   │   ├── render/      # IR → 渲染数据（虚拟分片）
│   │   ├── sanitize/    # HTML 消毒
│   │   └── store/       # 本地持久化（文件 + 索引 + 进度）
│   └── libs/            # 第三方（进分包或独立分包）
└── subpackages/
    ├── pkg-markdown/
    └── pkg-html/
```

## 5. 统一文档模型（IR，所有解析器的目标输出）
所有格式先解析成同一 IR，渲染层只认 IR：
```
Block[] = {
  type: 'heading' | 'paragraph' | 'code' | 'list' | 'table' |
        'image' | 'blockquote' | 'hr' | 'math' | 'raw-html',
  ...(type 相关字段)
}
```
- TXT：按行/段落 → `paragraph`（可选启发式识别标题/代码）。
- Markdown：AST → IR。
- HTML：消毒 + 解析 DOM 子集 → IR；复杂 CSS 布局失败时**降级为「阅读模式」**（提取正文 → IR）。

## 6. 质量门禁（每个 Phase 完成前必须通过）
1. 开发者工具 + **真机**（iOS/Android 各一台）预览通过。开发者工具结果不具代表性，尤其是性能与内存。
2. 核心路径在真机无白屏/无 crash。
3. 大文件样本（≥10MB GBK txt、含 base64 图片的 HTML、含公式/表格的 md）通过。
4. 安全用例：恶意 HTML（script/iframe/外链）被消毒，外链不自动跳转。
5. 包体积主包 < 2MB。
6. **Tailwind CDN 样例还原**：3 份使用 Tailwind 的真实 AI 文档，布局与配色基本还原（Phase 3 门禁，不过关则 Phase 3 不算完成）。
7. **滚动无跳动**：长文档上下滚动时不出现位置突跳（虚拟滚动高度补偿正确）。

## 6.5 合规硬要求（会卡上线，不是可选项）

- **《小程序用户隐私保护指引》必须配置**：`app.json` 设 `"__usePrivacyCheck__": true`，首次调用前走 `wx.requirePrivacyAuthorize`。`wx.chooseMessageFile` 与剪贴板读取**都属于需声明接口**，不配置会导致接口调用直接失败。用户拒绝授权时要给明确说明而非卡死。
- **必须内置 3 份示例文档**（md / html / txt 各一，自有版权内容），首屏提供「查看示例」入口。
  > ⚠️ 极易遗漏但直接决定过审：**审核员的微信里没有可选文件**，无法测试你的小程序，会直接驳回。
- **提审说明必须写明**："文件仅在用户手机本地解析渲染，全程不上传任何服务器，无云端存储、无文件分享功能。" 这是纯本地方案的合规红利，也是过审的核心说辞。
- **不做文件转发 / 分享链接 / 云端存储**——微信明确禁止无资质的网盘类小程序。只分享小程序卡片（对方点开自己选文件）。
- **不需要内容安全 API**：无上传、无 UGC、无云端存储，因此无内容审核义务。
- 类目建议：工具 → 文档处理 / 效率（个人主体可选类目有限，注册后需确认实际可选项）。

## 7. 更多细节
- 完整产品架构与 ADR：`docs/产品架构设计.md`
- 边界情况全清单：`docs/Edge-Cases-清单.md`
- 分阶段构建说明书（喂给 WorkBuddy 的指令）：`docs/WorkBuddy-构建说明书.md`
- **隐私口径的唯一来源：`docs/隐私说明.md`**
  —— 后台《用户隐私保护指引》的填写正文、完整数据清单、每条承诺对应的代码位置。
  改任何隐私相关文案（授权弹窗、设置页说明、提审说明）前先看这份。
- 官方文档核对结论与真机排查记录：`docs/verify-notes.md`
- 代码审计报告：`docs/代码审计-2026-08-15.md`
